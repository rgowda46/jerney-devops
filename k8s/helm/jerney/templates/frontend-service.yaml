apiVersion: v1
kind: Service

metadata:
  name: jerney-frontend
  namespace: {{ .Values.namespace }}

spec:
  type: NodePort

  selector:
    app.kubernetes.io/name: jerney-frontend

  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
