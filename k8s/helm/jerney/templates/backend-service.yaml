apiVersion: v1
kind: Service

metadata:
  name: jerney-backend
  namespace: {{ .Values.namespace }}

spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: jerney-backend

  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
